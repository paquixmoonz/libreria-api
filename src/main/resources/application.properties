spring.application.name=libreria-api

# Configuración base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/libreria_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Puerto del servidor
server.port=8080

# Configuración JWT (lo agregaremos después)
app.jwt.secret=claveSuperSeguraDeAlMenos32Caracteres123456789
app.jwt.expiration-ms=3600000
